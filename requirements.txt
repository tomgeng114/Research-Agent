# Research-Agent Dependencies
# Python 3.10+

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# LLM client (OpenAI-compatible — works with DeepSeek)
openai>=1.30.0

# Data validation
pydantic>=2.6.0

# HTTP (for potential external API calls)
httpx>=0.27.0

# Environment variables
python-dotenv>=1.0.0
